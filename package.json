{
  "name": "cutmakers",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "api": "pnpm --filter @cutmakers/api dev",
    "web": "pnpm --filter @cutmakers/web dev"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
